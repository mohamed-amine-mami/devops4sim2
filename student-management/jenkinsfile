pipeline {
    agent any

    tools {
        maven 'Maven-3'
        jdk 'JDK-17'
    }

    environment {
        DOCKER_IMAGE          = "student-management"
        DOCKERHUB_REPO        = "r3aad/student-management"
        K8S_NAMESPACE         = "devops"
        // Pour SonarQube qui tourne DANS le cluster Kubernetes
        SONAR_SERVICE         = "sonarqube-service"  // on créera ce service plus bas
    }

    stages {
        /* -------- CHECKOUT CODE -------- */
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/mohamed-amine-mami/devops4sim2.git'
            }
        }

        /* -------- VERIFY ENVIRONMENT -------- */
        stage('Verify Environment') {
            steps {
                sh 'mvn --version'
                sh 'java -version'
                sh 'docker --version'
                sh 'kubectl version --client'
            }
        }

        /* -------- MAVEN BUILD -------- */
        stage('Build Maven Package') {
            steps {
                dir('student-management') {
                    sh 'mvn -B clean package -DskipTests'
                }
            }
        }

        /* -------- SONARQUBE ANALYSIS (dans le cluster) -------- */
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('MySonar') {   // 'MySonar' doit être configuré dans Jenkins Global Config
                    dir('student-management') {
                        sh """
                        mvn sonar:sonar \
                          -Dsonar.projectKey=student-management \
                          -Dsonar.projectName="Student Management" \
                          -Dsonar.host.url=http://${SONAR_SERVICE}:9000
                        """
                    }
                }
            }
        }

        /* -------- QUALITY GATE -------- */
        stage('Quality Gate') {
            steps {
                timeout(time: 3, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

        /* -------- DOCKER BUILD & PUSH -------- */
        stage('Build & Push Docker Image') {
            steps {
                dir('student-management') {
                    sh "docker build -t ${DOCKER_IMAGE}:latest ."
                    
                    withCredentials([usernamePassword(
                        credentialsId: 'dockerhub',
                        usernameVariable: 'DOCKER_USER',
                        passwordVariable: 'DOCKER_PASS'
                    )]) {
                        sh '''
                        echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
                        docker tag  ${DOCKER_IMAGE}:latest ${DOCKERHUB_REPO}:latest
                        docker tag  ${DOCKER_IMAGE}:latest ${DOCKERHUB_REPO}:${BUILD_NUMBER}
                        docker push ${DOCKERHUB_REPO}:latest
                        docker push ${DOCKERHUB_REPO}:${BUILD_NUMBER}
                        '''
                    }
                }
            }
        }

        /* -------- DÉPLOIEMENT KUBERNETES (la partie qui manquait !) -------- */
        stage('Deploy to Kubernetes') {
            steps {
                script {
                    // On utilise l'image qu'on vient de pousser sur Docker Hub
                    sh """
                    kubectl -n ${K8S_NAMESPACE} set image deployment/spring-deployment \
                        spring=${DOCKERHUB_REPO}:${BUILD_NUMBER} --record
                    
                    kubectl -n ${K8S_NAMESPACE} rollout status deployment/spring-deployment --timeout=180s
                    """
                }
            }
        }

        /* -------- TEST FINAL DE L'APPLICATION -------- */
        stage('Test Application') {
            steps {
                script {
                    def url = sh(script: "minikube service spring-service -n ${K8S_NAMESPACE} --url | head -1", returnStdout: true).trim()
                    echo "URL de l'application : ${url}"
                    
                    // Test simple d'un endpoint (adapte selon ton API)
                    sh "curl -f ${url}/department/getAllDepartment || exit 1"
                    echo "Application accessible et fonctionnelle !"
                }
            }
        }
    }

    post {
        always {
            echo "Nettoyage du workspace..."
            cleanWs()
        }
        success {
            echo "PIPELINE TERMINÉ AVEC SUCCÈS - TOUT LE WORKSHOP EST VALIDÉ !"
        }
        failure {
            echo "ÉCHEC DU PIPELINE - Vérifie les logs"
        }
    }
}
