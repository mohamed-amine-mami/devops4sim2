pipeline {
    agent any

    tools {
        maven 'Maven-3'
    }

    stages {

        stage('Hello') {
            steps {
                echo 'Hello world'
            }
        }

        stage('Debug — workspace files') {
            steps {
                sh 'ls -R .'
            }
        }

        stage('Tests unitaires') {
            steps {
                dir('student-management') {
                    // Skip tests but run the stage
                    sh 'mvn -B test -DskipTests'
                }
            }
            post {
                always {
                    // No failure, even if no tests
                    junit allowEmptyResults: true, testResults: 'student-management/target/surefire-reports/*.xml'
                }
            }
        }

        stage('Création du livrable') {
            steps {
                dir('student-management') {
                    // Build WITHOUT running tests
                    sh 'mvn -B package -DskipTests'
                }
            }
        }

        stage('Archivage du livrable') {
            steps {
                archiveArtifacts artifacts: 'student-management/target/*.jar', fingerprint: true
            }
        }
    }
}
